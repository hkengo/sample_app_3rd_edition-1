<li id="micropost-<%= micropost.id %>">
  <%= link_to gravatar_for(micropost.user, size: 50), micropost.user %>
  <span class="user"><%= link_to micropost.user.name, micropost.user %></span>
  <span class="content">
    <%= micropost.content %>
    <%= image_tag micropost.picture.url if micropost.picture? %>
  </span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(micropost.created_at) %> ago.
    <% if current_user?(micropost.user) %>
      <%= link_to "delete", micropost, method: :delete,
                                       data: { confirm: "You sure?" } %>
    <% end %>
    
    <% unless micropost.is_reply? %>
    <%= form_tag(reply_micropost_path, remote: true) do %>
      <%= hidden_field_tag :reply_id, micropost.id %>
        <%= submit_tag "Reply" %>
      <% end %>
    <% end %>
  </span>
  <span id="reply_form_<%= micropost.id %>" class="reply">
  <%= render "shared/micropost_form", reply_id: micropost.id unless micropost.is_reply? %>
  </span>
</li>